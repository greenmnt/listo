# Same DB the Python scraper writes. Copy to .env (or just `export`) before
# `cargo run`.
DATABASE_URL=mysql://listo:password@localhost/listo
LISTO_API_BIND=0.0.0.0:8080
RUST_LOG=listo_api=info,tower_http=warn,sqlx=warn
